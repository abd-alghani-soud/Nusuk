import 'package:flutter/material.dart';

class AzkarAlnoom extends StatelessWidget {
  const AzkarAlnoom({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
